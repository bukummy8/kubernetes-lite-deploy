apiVersion: v1
kind: Service
metadata:
  name: kubernetes-lite-api
  labels:
    app: kubernetes-lite-api
spec:
  selector:
    app: kubernetes-lite-api
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort